## 1. README.md

- [x] 1.1 Create `README.md` with project description, prerequisites (Python 3.13, uv, Anthropic API key), setup instructions, usage (web app + CLI), and architecture overview listing all modules

## 2. CLAUDE.md

- [x] 2.1 Add `models.py` entry to the Architecture section describing the `CoffeeBean` dataclass and its methods
- [x] 2.2 Update Key Details section to note automatic database migration behavior in `init_db()`
