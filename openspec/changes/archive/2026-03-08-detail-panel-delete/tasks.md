## 1. Frontend

- [x] 1.1 Add a "Delete" button to the detail panel HTML, styled red, placed below the Save button
- [x] 1.2 Add click handler that confirms deletion, calls `DELETE /api/coffees/<id>`, closes the panel, and refreshes the list
